# START HERE

1. `AGENTS.md` を読み、正本 Python app を直接編集しない前提を確認する。
2. `README.md` で起動方法と現在の機能を確認する。
3. `docs/desktop-implementation-plan.md` で初回スコープと戻し方を確認する。
4. 動作確認は `python -m unittest discover -s tests` から始める。
5. UI を確認する場合は `python main.py`、起動失敗を追う場合は `debug-start.bat` を使う。
