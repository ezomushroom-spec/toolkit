# empirical-prompt-tuning 仕上げリスト

## 1. 位置づけ

`empirical-prompt-tuning` の導入、初回横展開、2nd iteration、`AGENTS.md` 主要節 trial まで終えた時点で、まだ閉じ切っていない項目だけを短く残す。

## 2. いま完了済みのこと

- `Critical` 群の初回 trial
- `Important` 群の初回 trial
- `Opportunistic` 群の初回 trial
- 主要 3 件の 2nd iteration
- root `AGENTS.md` の主要節 trial 開始
- 1 枚運用ルールの作成

## 3. 残りの未完了点

### A. 運用定着

1. guidance section trial をどこまで標準運用に含めるか固定する
2. `slash command` が追加された場合の canonical 置き場を決める

### B. 記録方式

1. duration / tool count を今後どこまで厳密に記録するか決める
2. `static review only` の記録書式をテンプレート化するか決める

### C. 外部反映

1. remote 設定後に push / PR を実行する
2. 必要なら project 側に例外理由や戻し方の未記載箇所を補う

## 4. 優先順

### まず閉じる

1. remote 設定後に push / PR を実行する
2. 必要なら `static review only` の定型を追加する

### 後でよい

1. home 側 skill や plugin skill への横展開
2. `slash command` 実体追加後の再分類
3. snapshot zip の定期整理

## 5. 完了条件

次を満たしたら、この段階の導入はひと区切りとみなせる。

- 1 枚運用ルールで次回の運用判断ができる
- result の残し方が決まっている
- 未完了項目が `open questions` か `finish list` のどちらかへ集約されている
- 外部反映するなら、その単位と例外理由が説明できる

## 6. 今のおすすめ

次にやるなら次の順が自然。

1. その後に commit / push / PR へ進むか決める
2. 必要なら `static review only` の定型を追加する
3. `slash command` 実体が出た時点で canonical 置き場を固定する
