# 計測スナップショット 2026-04

## 期間

- 2026-04

## 主な対象

- workspace ルール整理
- skill 棚卸し
- `workspace-template` の切り出し
- 実効性計測の前段整理

## 使われた skill

このスナップショット作成時点では、厳密集計はまだ始めていない。
ただし、`skill-usage-log-202604.md` を今月分の簡易ログとして作成し、以後は案件単位で短く残す。

運用改善や診断系では次の基幹 skill を優先対象としている。

- `request-translate`
- `pre-implementation-diagnosis`
- `implementation-planner`
- `plan-review`
- `code-review`

補足:

- 4 月時点では skill の棚卸しと統合を優先しており、利用頻度より入口整理を重く見ている

## 完了報告の充足度

現時点の所見:

- 完了報告では `何を変えたか` と `何を確認したか` は比較的残しやすい
- `未確認事項` と `残るリスク` は、軽い文書変更や整理作業では薄くなりやすい

4 月時点の暫定判断:

- 品質基準そのものは整っている
- ただし、軽作業でも `未確認事項` と `残るリスク` を短く残す癖づけはまだ必要

## docs 更新漏れ

現時点の所見:

- Phase 1 と Phase 2 の整理で、`README.md` から辿れる状態は改善した
- `workflow.md`、`quality-gates.md`、`AGENTS.md` の役割分担も以前より明確
- ただし、新しい補助文書が増えると入口更新を忘れる余地はまだある

4 月時点の暫定判断:

- docs 更新漏れは大きくは見えていない
- ただし、新規文書追加時に `docs/README.md` 更新を毎回見る運用は継続した方がよい

## よく出た詰まり

4 月時点で見えた詰まりの型:

- 役割が近い skill の境界が見えにくい
- 文書の総量が増えると、人間にとっての入口が分かりにくくなる
- 完了条件が複数文書に散らばると、どれを正本に見るか迷いやすい

該当分類:

- docs 更新漏れ
- 仕様不足
- 設計分離不足

## 今月の改善結果

- `AGENTS.md` をセクション化した
- `workflow.md` と `quality-gates.md` の役割分担を整理した
- `docs/README.md` の入口を強化した
- skill 棚卸しを行い、一部統合と削除を行った
- `workspace-template` を最小構成で切り出した
- 実効性計測の前段文書を追加した

## 次に直すべき運用上の弱点

1. `skill-usage-log-YYYYMM.md` を案件単位で埋め、次回スナップショットで偏りを見る
2. 軽作業でも `未確認事項` と `残るリスク` をゼロにはしない運用へ寄せる
3. `workspace-template` の可変項目をどこまで外出しするか、次段で整理する

## 備考

- このスナップショットは、計測運用の初回雛形を兼ねる
- 今回は定量集計より、何を見れば十分かの固定を優先している
