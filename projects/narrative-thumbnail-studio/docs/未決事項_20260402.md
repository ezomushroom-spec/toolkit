# 未決事項

## 実装前に決めるとよいこと

- `mediapipe` を今後復活予定の依存として残すのか、完全に外すのか
- 全プリセット出力で使うべき仕様を、現在表示中プリセット寄りにするのか、各プリセットの既定値寄りにするのか
- 履歴クリアを「一覧だけ消す」ではなく「永続履歴も削除する」操作として確定するか

## 実装しながら決めてよいこと

- EXIF 向き補正を `load_full()` と `load_thumbnail()` の両方に入れる実装詳細
- 履歴クリア時の確認文言
- 一括出力時のタグや source_paths の持ち方

## 次の調査候補

- `mediapipe` が起動時間や配布サイズにどの程度影響しているか
- 現在のテストスクリプトを自動アサーション付きに引き上げられるか
- 起動バッチ経由と `python main.py` 直実行で挙動差がないか

## 戻し方

- この project 配下の文書は判断記録のみなので、不要になれば project ごと退避できる
- 正本コードを変更する場合は、対象を `app/ui/` と `app/core/` に限定し、小さい単位で戻せるように分けて実装する
